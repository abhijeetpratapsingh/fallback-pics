# Validation Contract: Refresh README Authority Signals

The implementation passes when:

- The story acceptance criteria are observable in source or rendered output.
- HTML page URLs resolve without redirect in sitemap and internal links.
- /api/v1 image examples remain working image URLs.
- JSON-LD parses as valid JSON and avoids disallowed schema types.
- Security headers are present in deployment header config for web pages.
- Measurement/link artifacts contain no credentials and no unsupported claims.
