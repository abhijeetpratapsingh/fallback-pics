# Research: Finalize Sitemap URL Consistency

## Decisions

- Use the existing Astro static route behavior and Cloudflare Pages public-file conventions.
- Use Node scripts with built-in fetch for local checks.
- Use final trailing-slash HTML URLs and /api/v1 image URLs.
- Keep measurement and link acquisition artifacts as markdown so they can be reviewed before external use.
